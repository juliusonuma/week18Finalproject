spring:
   datasource:
      username: book_shop
      password: book_shop
      url: jdbc:mysql://localhost:3306/book_shop
   
   jpa:
      hibernate:
         ddl-auto: update
      show-sql: true 
      defer-datasource-initialization: true
      
   sql:
      init:
         mode: always

